#!/usr/bin/expect -f
set timeout 30
log_file -a "remote_fix2.log"
spawn ssh -o StrictHostKeyChecking=no root@84.247.143.180
expect {
    "password:" {
        send "Henrico9516\r"
    }
    timeout {
        puts "Connection timeout"
        exit 1
    }
}
expect -re ".*#.*|.*\\$.*"
send "find /var/www /home /opt -name api.js 2>/dev/null | grep src/services\r"
expect -re ".*#.*|.*\\$.*"
send "exit\r"
expect eof
